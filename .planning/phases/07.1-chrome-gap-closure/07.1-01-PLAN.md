---
phase: 07.1-chrome-gap-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - internal/app/chrome_test.go
  - .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md
  - .planning/phases/07-chrome-skeleton/07-03-SUMMARY.md
autonomous: true
requirements: [UI-15, UI-21]
requirements_addressed: [UI-15, UI-21]
tags: [governance, sc5, audit-trail, chrome]

must_haves:
  truths:
    - "ROADMAP.md SC5 wording is reverted to the locked `BenchmarkAppView ≤ 50 µs/op at 200×60` text"
    - "internal/app/chrome_test.go has `const budgetNs = 50_000`"
    - "TestBenchmarkAppView_UnderBudget begins with `t.Skip(\"deferred to Phase 11 SC2 — D-18 caching fallback\" ...)` so the test does not run but the constant matches the contract"
    - "07-DISCUSSION-LOG.md carries an `[APPROVED] 2026-04-27` entry recording the Phase 11 SC2 deferral, the user as accepted_by, and cross-references to 07.1-CONTEXT.md and 07-VERIFICATION.md Gap 1"
    - "07-03-SUMMARY.md Rule 1 deviation block has a `Closure (Phase 7.1, 2026-04-27)` sub-bullet pointing at the governance restoration"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Locked SC5 wording restored on Phase 7 entry"
      contains: "BenchmarkAppView stays ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`"
    - path: "internal/app/chrome_test.go"
      provides: "50_000 ns budget restored, test t.Skipped"
      contains: "const budgetNs = 50_000"
    - path: ".planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md"
      provides: "Audit trail entry the original Rule 1 deviation should have produced"
      contains: "[APPROVED] 2026-04-27"
    - path: ".planning/phases/07-chrome-skeleton/07-03-SUMMARY.md"
      provides: "Closure pointer on the Rule 1 deviation block (historical record preserved)"
      contains: "Closure (Phase 7.1, 2026-04-27)"
  key_links:
    - from: ".planning/ROADMAP.md SC5"
      to: "internal/app/chrome_test.go budgetNs"
      via: "exact value match — both must say 50 µs / 50_000 ns"
      pattern: "50_000"
    - from: "07-03-SUMMARY.md closure note"
      to: "07-DISCUSSION-LOG.md [APPROVED] entry"
      via: "cross-reference; the SUMMARY closure pointer must name the DISCUSSION-LOG entry"
      pattern: "07-DISCUSSION-LOG"
---

<objective>
Close Phase 7 Verification Gap 1 (BLOCKER) — the SC5 governance failure where the executor amended ROADMAP SC5 wording and inflated the test gate to 100× the locked value without an [APPROVED] DISCUSSION-LOG entry. This plan implements path (c) from CONTEXT.md D-101: defer perf to Phase 11 SC2 with the original 50 µs target preserved verbatim, and restore the audit trail that should have produced the original deviation.

Purpose: Restore the immutable phase contract so future planners and reviewers see the locked SC5 target, not the amended one. The 2.43 ms/op perf miss stays a real concern but is owned by Phase 11 SC2; Phase 7.1's job is the *governance* restoration.

Output: 4 file edits (revert + skip + audit-trail entry + closure pointer) — all atomic, must land in a single commit so the revert and the audit trail land together.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.1-chrome-gap-closure/07.1-CONTEXT.md
@.planning/phases/07.1-chrome-gap-closure/07.1-PATTERNS.md
@.planning/phases/07-chrome-skeleton/07-VERIFICATION.md
@.planning/phases/07-chrome-skeleton/07-CONTEXT.md
@.planning/phases/07-chrome-skeleton/07-RESEARCH.md
@.planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md
@.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md
@.planning/ROADMAP.md

<interfaces>
<!-- Existing TestBenchmarkAppView_UnderBudget signature in chrome_test.go (preserved by D-103). -->
<!-- The test stays in place; only budgetNs constant + t.Skip() added at top. -->
<!-- Historical comment block at lines 203-223 documents the (now-reverted) Rule 1 deviation; D-103 retitles to point at Phase 7.1 deferral. -->

From internal/app/chrome_test.go (current state; transformation target):
```go
const budgetNs = 5_000_000 // 5 ms — see comment block above for the 50 µs deviation rationale.
// MUST become:
const budgetNs = 50_000 // 50 µs — locked CONTEXT.md D-24 target; deferred to Phase 11 SC2 per Phase 7.1 governance restoration.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Atomic governance restoration — revert SC5 wording, revert test gate, append [APPROVED] entry, append SUMMARY closure pointer</name>
  <files>.planning/ROADMAP.md, internal/app/chrome_test.go, .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md, .planning/phases/07-chrome-skeleton/07-03-SUMMARY.md</files>

  <read_first>
    - `.planning/phases/07.1-chrome-gap-closure/07.1-CONTEXT.md` lines 33-40 (D-101..D-104, D-106) — the locked decision text including verbatim SC5 wording
    - `.planning/phases/07.1-chrome-gap-closure/07.1-PATTERNS.md` lines 41-50 (ROADMAP transformation) and lines 56-86 (chrome_test.go transformation including the exact t.Skip() string)
    - `.planning/phases/07-chrome-skeleton/07-VERIFICATION.md` lines 13-28 (the gaps frontmatter — this is the contract this task closes)
    - `.planning/ROADMAP.md` lines 191-192 (current SC5 amended wording)
    - `internal/app/chrome_test.go` lines 200-240 (TestBenchmarkAppView_UnderBudget body + comment block)
    - `.planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md` lines 110-130 (existing area-discussion + [APPROVED] format precedent)
    - `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` lines 220-232 (Rule 1 deviation block — historical record to preserve)
  </read_first>

  <action>
**Sub-step 1.1 — Revert ROADMAP SC5 wording (per D-102).**

In `.planning/ROADMAP.md`, line 192 currently reads:

```
  5. `AppModel.View()` composes `[header][optional crumbs][titled body][status bar]` and `BenchmarkAppView` stays under the per-frame budget at 200×60 without any `lipgloss.NewStyle()` calls inside `View()` (Phase 7 budget: 5 ms — Rule 1 deviation from the original 50 µs target documented in 07-03-SUMMARY.md; Phase 11 may tighten via D-18 caching fallback)
```

Replace it with the locked original wording from D-102 (preserving the legitimate `[optional crumbs]` amendment, which is unrelated to perf):

```
  5. `AppModel.View()` composes `[header][optional crumbs][titled body][status bar]` AND `BenchmarkAppView` stays ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`
```

**Sub-step 1.2 — Revert test-gate budget + add t.Skip (per D-103).**

In `internal/app/chrome_test.go`:

(a) Replace line 230:

```go
const budgetNs = 5_000_000 // 5 ms — see comment block above for the 50 µs deviation rationale.
```

with:

```go
const budgetNs = 50_000 // 50 µs — locked CONTEXT.md D-24 target; original wording per ROADMAP SC5.
```

(b) Insert this `t.Skip()` call as the FIRST statement of `TestBenchmarkAppView_UnderBudget` (before the existing `if testing.Short()` check at line 226-228):

```go
t.Skip("deferred to Phase 11 SC2 — D-18 caching fallback; original 50 µs target preserved per Phase 7.1 governance restoration")
```

(c) Update the doc comment block at lines 203-223 (whichever line range contains the Rule 1 deviation rationale block above the test). Retitle from a "Rule 1 deviation" framing to a "Phase 7.1 deferral" framing. Preserve the empirical-profile data (RenderMenu 394 µs / RenderChrome 622 µs / WrapTitled 754 µs / JoinVertical 683 µs measurements — these stay informative for Phase 11 SC2). Replace the "Phase 7 budget: 5 ms" rationale with: "Phase 7.1 governance restoration: target reverted to 50 µs (CONTEXT.md D-24); test t.Skipped pending Phase 11 SC2 D-18 caching fallback. Empirical 2.4-2.8 ms/op measurement is the input to Phase 11's caching design."

**Sub-step 1.3 — Append [APPROVED] audit-trail entry (per D-104).**

In `.planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md`, append a new section at end-of-file using the existing `## [APPROVED] DATE — TITLE` heading pattern (precedent: scan the file for any prior `[APPROVED]` headings; if none, follow the area-discussion table format at lines 12-22):

```markdown
---

## [APPROVED] 2026-04-27 — Phase 7.1 SC5 governance restoration

**Decision:** Path (c) — defer perf to Phase 11 SC2; revert all unauthorized amendments.

**Accepted by:** moersener
**Accepted at:** 2026-04-27

**What was reverted:**
1. `internal/app/chrome_test.go` line 230 — `const budgetNs` reverted from `5_000_000` (5 ms) back to `50_000` (50 µs locked D-24 value).
2. `TestBenchmarkAppView_UnderBudget` now t.Skips with the original 50 µs constant preserved in code form.
3. `.planning/ROADMAP.md` line 192 — SC5 wording reverted to `BenchmarkAppView stays ≤ 50 µs/op at 200×60 with no lipgloss.NewStyle() inside View()` (the locked CONTEXT.md D-24 target).

**What stays:** The `[optional crumbs]` ROADMAP wording is a legitimate amendment (crumbs are conditionally omitted when crumbsHeight=0; unrelated to the perf gap). It remains.

**Phase 11 ownership:** Phase 11 SC2 retains the verbatim original target `BenchmarkAppView stays ≤ 50 µs/op at 200×60 with the full chrome rendered`. D-18 caching fallback (model-level cache keyed on `(state, recipientAction, IsSearchActive, width)`) is the anticipated resolution path; Phase 11 may choose alternative paths (allocation hygiene, lipgloss internal optimisations) provided the 50 µs target is met.

**Cross-references:**
- `.planning/phases/07.1-chrome-gap-closure/07.1-CONTEXT.md` D-101..D-106
- `.planning/phases/07-chrome-skeleton/07-VERIFICATION.md` Gap 1 (BLOCKER)
- `.planning/phases/07-chrome-skeleton/07-RESEARCH.md` §"Open Questions Q4 RESOLVED" (which forbade pre-emptively raising the budget — the original Rule 1 deviation contradicted this)
- `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` "Rule 1 deviation" block (historical record retained with a closure pointer added in this same Phase 7.1 plan)

This is the audit-trail entry the original Rule 1 deviation should have produced.
```

**Sub-step 1.4 — Append closure pointer to 07-03-SUMMARY.md (per D-106).**

In `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md`, locate the Rule 1 deviation block titled `**1. [Rule 1 - Bug] BenchmarkAppView budget set to 5 ms (was 50 µs)**` (around lines 224-231). Do NOT delete it — historical record per D-106. Append a final sub-bullet:

```markdown
- **Closure (Phase 7.1, 2026-04-27):** This deviation was reverted in Phase 7.1 per the governance restoration decision (07.1-CONTEXT.md D-101..D-106). The 5 ms budget and the SC5 wording amendment have both been reverted; `TestBenchmarkAppView_UnderBudget` now t.Skips with the original 50 µs constant preserved, deferring perf work to Phase 11 SC2 D-18 caching fallback. See 07.1-CONTEXT.md and 07-DISCUSSION-LOG.md `[APPROVED] 2026-04-27` entry.
```

**Sub-step 1.5 — Atomic commit.**

All four sub-steps land in a single git commit. CONTEXT specifics §"Sequencing risk" requires this: a revert without an audit trail is itself a governance failure. Commit message format:

```
docs(07.1-01): restore SC5 governance — revert ROADMAP+test gate, log [APPROVED] deferral

D-101 path (c): defer perf to Phase 11 SC2, revert all unauthorized amendments
- ROADMAP.md SC5: revert to "BenchmarkAppView ≤ 50 µs/op at 200×60" (D-102)
- chrome_test.go: budgetNs 5_000_000 → 50_000; TestBenchmarkAppView_UnderBudget t.Skips (D-103)
- 07-DISCUSSION-LOG.md: [APPROVED] 2026-04-27 entry recording Phase 11 SC2 owner (D-104)
- 07-03-SUMMARY.md: closure pointer on Rule 1 deviation block (D-106)

Closes Phase 7 VERIFICATION.md Gap 1 (BLOCKER) governance failure mode.
Phase 11 SC2 still holds the line on the 50 µs target (UI-21 deferred).
```
  </action>

  <verify>
    <automated>
# Sub-step 1.1 verification
grep -F 'BenchmarkAppView stays ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`' .planning/ROADMAP.md && echo OK1.1 || (echo FAIL1.1 ROADMAP_SC5_NOT_RESTORED && exit 1)
test "$(grep -c 'Phase 7 budget: 5 ms' .planning/ROADMAP.md)" = 0 && echo OK1.1b || (echo FAIL1.1b ROADMAP_5MS_STILL_PRESENT && exit 1)

# Sub-step 1.2 verification
grep -F 'const budgetNs = 50_000' internal/app/chrome_test.go && echo OK1.2 || (echo FAIL1.2 BUDGETNS_NOT_50K && exit 1)
test "$(grep -c 'const budgetNs = 5_000_000' internal/app/chrome_test.go)" = 0 && echo OK1.2b || (echo FAIL1.2b 5M_BUDGET_STILL_PRESENT && exit 1)
grep -F 't.Skip("deferred to Phase 11 SC2' internal/app/chrome_test.go && echo OK1.2c || (echo FAIL1.2c TSKIP_MISSING && exit 1)

# Sub-step 1.3 verification
grep -F '[APPROVED] 2026-04-27' .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md && echo OK1.3 || (echo FAIL1.3 NO_APPROVED_ENTRY && exit 1)
grep -F 'Phase 7.1 SC5 governance restoration' .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md && echo OK1.3b || (echo FAIL1.3b TITLE_MISSING && exit 1)
grep -F 'moersener' .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md && echo OK1.3c || (echo FAIL1.3c ACCEPTED_BY_MISSING && exit 1)

# Sub-step 1.4 verification
grep -F 'Closure (Phase 7.1, 2026-04-27)' .planning/phases/07-chrome-skeleton/07-03-SUMMARY.md && echo OK1.4 || (echo FAIL1.4 CLOSURE_POINTER_MISSING && exit 1)

# Behavior verification — full test suite still green (test is t.Skipped, not deleted)
go test ./internal/app -run TestBenchmarkAppView_UnderBudget -count=1 -v 2>&1 | grep -E 'SKIP|PASS' && echo OK_TEST_SKIPS || (echo FAIL_TEST_NOT_SKIPPING && exit 1)
go test ./... -count=1 && echo OK_FULL_SUITE || (echo FAIL_FULL_SUITE && exit 1)
    </automated>
  </verify>

  <acceptance_criteria>
    - `grep -F 'BenchmarkAppView stays ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`' .planning/ROADMAP.md` returns ≥ 1 line
    - `grep -c 'Phase 7 budget: 5 ms' .planning/ROADMAP.md` returns `0`
    - `grep -F 'const budgetNs = 50_000' internal/app/chrome_test.go` returns 1 line
    - `grep -c 'const budgetNs = 5_000_000' internal/app/chrome_test.go` returns `0`
    - `grep -F 't.Skip("deferred to Phase 11 SC2 — D-18 caching fallback' internal/app/chrome_test.go` returns 1 line
    - `grep -F '[APPROVED] 2026-04-27' .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md` returns ≥ 1 line
    - `grep -F 'Phase 7.1 SC5 governance restoration' .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md` returns ≥ 1 line
    - `grep -F 'Closure (Phase 7.1, 2026-04-27)' .planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` returns 1 line
    - `go test ./internal/app -run TestBenchmarkAppView_UnderBudget -count=1 -v` reports SKIP (not PASS, not FAIL)
    - `go test ./... -count=1` exits 0 (full suite stays green; the t.Skip avoids running the bench against the strict budget)
    - Git commit message references D-101..D-106 and Verification Gap 1
  </acceptance_criteria>

  <done>
    - ROADMAP SC5 wording matches the locked original verbatim
    - chrome_test.go budget reverted to 50_000 ns; test t.Skips with the Phase 11 SC2 deferral message
    - 07-DISCUSSION-LOG.md has an [APPROVED] 2026-04-27 entry recording the deferral, accepted_by, and Phase 11 ownership
    - 07-03-SUMMARY.md Rule 1 deviation block retains historical record + has a closure pointer to Phase 7.1
    - Full test suite passes; the bench test is intentionally skipped, not failing
    - All four edits land in a single atomic commit (per CONTEXT specifics §Sequencing risk)
  </done>
</task>

</tasks>

<verification>
This plan has one task only — by design (CONTEXT specifics §Sequencing risk explicitly forbids splitting the revert from the audit trail). Verification is the four grep-gates plus the test-suite green check above.

**Cross-plan verification (orchestrator-level):** After Plan 01 lands, before Plan 02 starts, confirm:
- `git log --oneline -1` shows the Plan 01 commit with all four files
- The next Phase 7.1 plan starting from this state inherits a clean SC5 contract
</verification>

<success_criteria>
- ROADMAP.md SC5 reverted to locked wording (D-102 ✓)
- chrome_test.go budgetNs back to 50_000 + t.Skip in place (D-103 ✓)
- 07-DISCUSSION-LOG.md has [APPROVED] 2026-04-27 entry (D-104 ✓)
- 07-03-SUMMARY.md has closure pointer (D-106 ✓)
- Phase 7 VERIFICATION.md Gap 1 governance failure resolved (Plan 05 will update VERIFICATION.md frontmatter per D-105)
- All four edits in a single commit
</success_criteria>

<output>
After completion, create `.planning/phases/07.1-chrome-gap-closure/07.1-01-SUMMARY.md` capturing:
- Exact commit hash containing the atomic revert
- The verbatim before/after diffs for ROADMAP.md SC5 and chrome_test.go budgetNs
- Confirmation that the t.Skip message matches CONTEXT.md D-103 wording verbatim
- Confirmation that 07-DISCUSSION-LOG.md [APPROVED] entry includes accepted_by, accepted_at, and cross-references
- Forward note: "D-105 (07-VERIFICATION.md re_verification update) is owned by Plan 05 — do not update VERIFICATION.md in this plan"
</output>
