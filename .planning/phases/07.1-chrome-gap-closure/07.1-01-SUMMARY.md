---
phase: 07.1-chrome-gap-closure
plan: 01
subsystem: governance
tags: [governance, sc5, audit-trail, chrome, roadmap-revert, t-skip-deferral]

# Dependency graph
requires:
  - phase: 07-chrome-skeleton (Plan 03)
    provides: TestBenchmarkAppView_UnderBudget infrastructure (the test whose budgetNs constant Phase 7.1-01 reverts) + Rule 1 deviation block in 07-03-SUMMARY (the historical record Phase 7.1-01 amends with a closure pointer)
  - phase: 07.1-chrome-gap-closure (planning artifacts)
    provides: 07.1-CONTEXT.md D-101..D-106 (governance restoration decisions); 07.1-PATTERNS.md (chrome_test.go transformation recipe)
provides:
  - "ROADMAP SC5 wording restored to locked CONTEXT.md D-24 verbatim — `BenchmarkAppView ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`"
  - "internal/app/chrome_test.go::budgetNs reverted to 50_000 (50 µs) — contract preserved in code form"
  - "TestBenchmarkAppView_UnderBudget t.Skips with verbatim deferral message — preserves test for Phase 11 SC2 to un-skip when D-18 caching lands"
  - "07-DISCUSSION-LOG.md `[APPROVED] 2026-04-27 — Phase 7.1 SC5 governance restoration` entry — the audit trail the original Rule 1 deviation should have produced"
  - "07-03-SUMMARY.md Rule 1 deviation block has a Closure (Phase 7.1, 2026-04-27) sub-bullet pointing at the governance restoration; historical record retained per D-106"
  - "Phase 7 VERIFICATION.md Gap 1 (BLOCKER) governance failure mode resolved — perf concern is now formally Phase 11 SC2's responsibility (UI-21)"
affects:
  - 07-VERIFICATION.md (Plan 05 will flip status: gaps_found → gaps_closed_by_phase_7.1 and add this gap to re_verification.gaps_closed per D-105 — explicitly NOT this plan)
  - 11-regression-perf-gates (Phase 11 SC2 retains the verbatim 50 µs target; D-18 caching fallback is the anticipated resolution path; Phase 11 may un-skip TestBenchmarkAppView_UnderBudget once the budget can be met)

# Tech tracking
tech-stack:
  added: []  # No new dependencies — pure governance / doc / test-skip change
  patterns:
    - "Pattern: t.Skip with deferral message at top of test function preserves the contract in code form while gating execution — Phase 11 un-skips when ready (vs deletion which requires recreation)"
    - "Pattern: [APPROVED] DATE — TITLE heading in DISCUSSION-LOG.md as audit-trail entry — codifies the user's deferral decision with accepted_by, accepted_at, cross-references"
    - "Pattern: Closure pointer sub-bullet on a historical Rule 1 deviation block — preserves the original deviation rationale (historical record) while pointing at its eventual resolution"
    - "Pattern: Single atomic commit for revert + audit trail — splitting them risks an interim state where the revert lands without its accompanying audit-trail entry, which is itself a governance failure (CONTEXT specifics §Sequencing risk)"

key-files:
  created: []  # No new files — pure modifications
  modified:
    - .planning/ROADMAP.md (1 line — SC5 wording reverted to locked verbatim text per D-102)
    - internal/app/chrome_test.go (~25 lines — budgetNs 5_000_000 → 50_000; t.Skip added at top of TestBenchmarkAppView_UnderBudget; doc comment block reframed from Rule 1 deviation to Phase 7.1 governance restoration with empirical profile retained for Phase 11)
    - .planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md (~25 lines appended — [APPROVED] 2026-04-27 entry per D-104)
    - .planning/phases/07-chrome-skeleton/07-03-SUMMARY.md (1 line appended — Closure (Phase 7.1, 2026-04-27) sub-bullet on Rule 1 deviation block per D-106)

key-decisions:
  - "Path (c) implementation: defer perf to Phase 11 SC2 via t.Skip — preserves the locked 50 µs constant in code form; Phase 11 un-skips when D-18 caching lands or alternative perf path meets target"
  - "All 4 edits land in a single atomic commit (444df3b) per CONTEXT specifics §Sequencing risk — a revert without its audit trail is itself a governance failure"
  - "Doc comment block in chrome_test.go reframed from Rule 1 deviation to Phase 7.1 governance restoration but the empirical profile (RenderMenu 394 µs / RenderChrome 622 µs / WrapTitled 754 µs / JoinVertical 683 µs / total ~2.4-2.8 ms/op at 200×60) was retained — informative input to Phase 11 SC2's caching design"
  - "Forward note to Plan 05: D-105 (07-VERIFICATION.md re_verification update) is explicitly Plan 05's responsibility — this plan only closes the governance gap; Plan 05 closes the verification record"

requirements-completed: [UI-15]  # UI-21 stays formally deferred to Phase 11 SC2

# Metrics
duration: ~5min
completed: 2026-04-27
---

# Phase 7.1 Plan 01: Restore SC5 Governance — Atomic Revert Summary

**Phase 7 verification BLOCKER (Gap 1) closed via path (c) deferral: locked SC5 wording restored on ROADMAP.md, `TestBenchmarkAppView_UnderBudget` budget reverted from 5,000,000 to 50,000 ns and t.Skipped pending Phase 11 SC2, audit-trail [APPROVED] 2026-04-27 entry appended to 07-DISCUSSION-LOG.md, and 07-03-SUMMARY.md historical Rule 1 deviation block carries a Phase 7.1 closure pointer. All four edits land in a single commit (444df3b) per CONTEXT §Sequencing risk.**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-04-27T14:08:00Z (first edit after context load)
- **Completed:** 2026-04-27T14:11:34Z
- **Tasks:** 1 (atomic — by plan design; CONTEXT specifics §Sequencing risk explicitly forbids splitting the revert from the audit trail)
- **Files modified:** 4
- **Files created:** 0
- **LOC delta:** +51 / -18

## Commit

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Atomic governance restoration — revert SC5 wording, revert test gate, append [APPROVED] entry, append SUMMARY closure pointer | `444df3b` | `.planning/ROADMAP.md`, `internal/app/chrome_test.go`, `.planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md`, `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` |

## Verbatim Before/After Diffs

### `.planning/ROADMAP.md` SC5 (line 192)

**Before (amended; from Phase 7 commit 02273eb):**

```
  5. `AppModel.View()` composes `[header][optional crumbs][titled body][status bar]` and `BenchmarkAppView` stays under the per-frame budget at 200×60 without any `lipgloss.NewStyle()` calls inside `View()` (Phase 7 budget: 5 ms — Rule 1 deviation from the original 50 µs target documented in 07-03-SUMMARY.md; Phase 11 may tighten via D-18 caching fallback)
```

**After (locked CONTEXT.md D-24 verbatim wording, with the legitimate `[optional crumbs]` amendment retained per D-102):**

```
  5. `AppModel.View()` composes `[header][optional crumbs][titled body][status bar]` AND `BenchmarkAppView` stays ≤ 50 µs/op at 200×60 with no `lipgloss.NewStyle()` inside `View()`
```

### `internal/app/chrome_test.go::budgetNs` (line 236, was 230 before doc-block expansion)

**Before:**

```go
const budgetNs = 5_000_000 // 5 ms — see comment block above for the 50 µs deviation rationale.
```

**After:**

```go
const budgetNs = 50_000 // 50 µs — locked CONTEXT.md D-24 target; original wording per ROADMAP SC5.
```

### `internal/app/chrome_test.go::TestBenchmarkAppView_UnderBudget` first statement (line 230)

**Before (no top-level skip; only `testing.Short()` short-circuit):**

```go
func TestBenchmarkAppView_UnderBudget(t *testing.T) {
	if testing.Short() {
		t.Skip("bench-budget skipped in -short mode")
	}
	result := testing.Benchmark(BenchmarkAppView)
	...
```

**After (unconditional t.Skip with verbatim D-103 message; subsequent body preserved so Phase 11 can un-skip by deleting the one line):**

```go
func TestBenchmarkAppView_UnderBudget(t *testing.T) {
	t.Skip("deferred to Phase 11 SC2 — D-18 caching fallback; original 50 µs target preserved per Phase 7.1 governance restoration")
	if testing.Short() {
		t.Skip("bench-budget skipped in -short mode")
	}
	result := testing.Benchmark(BenchmarkAppView)
	...
```

### t.Skip message verbatim alignment with CONTEXT.md D-103

CONTEXT.md D-103 specifies:

> Add `t.Skip("deferred to Phase 11 SC2 — D-18 caching fallback; original 50 µs target preserved per Phase 7.1 governance restoration")` at the top of `TestBenchmarkAppView_UnderBudget`.

Implementation matches **byte-for-byte**:

```go
t.Skip("deferred to Phase 11 SC2 — D-18 caching fallback; original 50 µs target preserved per Phase 7.1 governance restoration")
```

Confirmed via:

```bash
$ grep -F 't.Skip("deferred to Phase 11 SC2 — D-18 caching fallback; original 50 µs target preserved per Phase 7.1 governance restoration")' internal/app/chrome_test.go
```

returns 1 line (the t.Skip statement at line 230).

### `07-DISCUSSION-LOG.md` audit-trail entry (appended at end)

```markdown
---

## [APPROVED] 2026-04-27 — Phase 7.1 SC5 governance restoration

**Decision:** Path (c) — defer perf to Phase 11 SC2; revert all unauthorized amendments.

**Accepted by:** moersener
**Accepted at:** 2026-04-27

**What was reverted:**
1. `internal/app/chrome_test.go` line 230 — `const budgetNs` reverted from `5_000_000` (5 ms) back to `50_000` (50 µs locked D-24 value).
2. `TestBenchmarkAppView_UnderBudget` now t.Skips with the original 50 µs constant preserved in code form.
3. `.planning/ROADMAP.md` line 192 — SC5 wording reverted to `BenchmarkAppView stays ≤ 50 µs/op at 200×60 with no lipgloss.NewStyle() inside View()` (the locked CONTEXT.md D-24 target).

**What stays:** The `[optional crumbs]` ROADMAP wording is a legitimate amendment (crumbs are conditionally omitted when crumbsHeight=0; unrelated to the perf gap). It remains.

**Phase 11 ownership:** Phase 11 SC2 retains the verbatim original target `BenchmarkAppView stays ≤ 50 µs/op at 200×60 with the full chrome rendered`. D-18 caching fallback (model-level cache keyed on `(state, recipientAction, IsSearchActive, width)`) is the anticipated resolution path; Phase 11 may choose alternative paths (allocation hygiene, lipgloss internal optimisations) provided the 50 µs target is met.

**Cross-references:**
- `.planning/phases/07.1-chrome-gap-closure/07.1-CONTEXT.md` D-101..D-106
- `.planning/phases/07-chrome-skeleton/07-VERIFICATION.md` Gap 1 (BLOCKER)
- `.planning/phases/07-chrome-skeleton/07-RESEARCH.md` §"Open Questions Q4 RESOLVED" (which forbade pre-emptively raising the budget — the original Rule 1 deviation contradicted this)
- `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` "Rule 1 deviation" block (historical record retained with a closure pointer added in this same Phase 7.1 plan)

This is the audit-trail entry the original Rule 1 deviation should have produced.
```

**Required-fields confirmation per D-104:**

- [x] `[APPROVED] 2026-04-27` heading present
- [x] `Accepted by: moersener` present
- [x] `Accepted at: 2026-04-27` present
- [x] Cross-reference to `07.1-CONTEXT.md D-101..D-106` present
- [x] Cross-reference to `07-VERIFICATION.md` Gap 1 present
- [x] Cross-reference to `07-03-SUMMARY.md` Rule 1 deviation block present
- [x] Phase 11 SC2 owner identified for the perf work

### `07-03-SUMMARY.md` Rule 1 closure sub-bullet (appended to existing block; historical record preserved per D-106)

```markdown
- **Closure (Phase 7.1, 2026-04-27):** This deviation was reverted in Phase 7.1 per the governance restoration decision (07.1-CONTEXT.md D-101..D-106). The 5 ms budget and the SC5 wording amendment have both been reverted; `TestBenchmarkAppView_UnderBudget` now t.Skips with the original 50 µs constant preserved, deferring perf work to Phase 11 SC2 D-18 caching fallback. See 07.1-CONTEXT.md and 07-DISCUSSION-LOG.md `[APPROVED] 2026-04-27` entry.
```

## Verification Results

All grep-gates and behavior verifications from the plan's `<verify><automated>` block pass:

| Sub-step | Verification | Result |
| -------- | ------------ | ------ |
| 1.1 | `grep -F 'BenchmarkAppView` stays ≤ 50 µs/op at 200×60' .planning/ROADMAP.md` (sub-grep around the literal SC5 fragment) | ✓ PASS — line 192 matches the locked verbatim wording |
| 1.1b | `grep -c 'Phase 7 budget: 5 ms' .planning/ROADMAP.md` | ✓ 0 — amended language fully removed |
| 1.2 | `grep -F 'const budgetNs = 50_000' internal/app/chrome_test.go` | ✓ PASS |
| 1.2b | `grep -c 'const budgetNs = 5_000_000' internal/app/chrome_test.go` | ✓ 0 |
| 1.2c | `grep -F 't.Skip("deferred to Phase 11 SC2' internal/app/chrome_test.go` | ✓ PASS — verbatim D-103 message |
| 1.3 | `grep -F '[APPROVED] 2026-04-27' 07-DISCUSSION-LOG.md` | ✓ PASS |
| 1.3b | `grep -F 'Phase 7.1 SC5 governance restoration' 07-DISCUSSION-LOG.md` | ✓ PASS |
| 1.3c | `grep -F 'moersener' 07-DISCUSSION-LOG.md` | ✓ PASS |
| 1.4 | `grep -F 'Closure (Phase 7.1, 2026-04-27)' 07-03-SUMMARY.md` | ✓ PASS |
| Behavior | `go test ./internal/app -run TestBenchmarkAppView_UnderBudget -count=1 -v` reports SKIP | ✓ SKIP — `--- SKIP: TestBenchmarkAppView_UnderBudget (0.00s)` with deferral message |
| Behavior | `go build ./...` exits 0 | ✓ PASS |
| Behavior | `go vet ./internal/app/...` exits 0 | ✓ PASS |
| Behavior | `go test ./internal/app -count=1` exits 0 | ✓ PASS |
| Behavior | `go test ./... -count=1` exits 0 (full repo) | ✓ PASS |

The earlier shell-quoted grep for SC5 (using backticks inside single-quoted strings) reported FAIL1.1, but inspection confirmed the substring is present byte-for-byte at line 192 — the failure was a shell-quoting artefact in the verification command, not a content failure. Re-grep with simpler quoting confirms the SC5 wording is correctly restored.

## Decisions Made

This plan implemented the locked decisions from `07.1-CONTEXT.md` D-101..D-106 verbatim. No deviation. All four sub-steps landed in a single commit per CONTEXT specifics §Sequencing risk.

## Deviations from Plan

None — plan executed exactly as written.

The plan was a single atomic task (by design) and required four file edits all landing in a single commit. Implementation matches the plan's `<action>` block byte-for-byte for the t.Skip message, the budgetNs constant value, the [APPROVED] heading text, and the closure pointer sub-bullet.

The doc comment block above `TestBenchmarkAppView_UnderBudget` was reframed from "Rule 1 deviation" framing to "Phase 7.1 governance restoration" framing per D-103 sub-step (c) — the empirical perf profile (RenderMenu 394 µs / RenderChrome 622 µs / WrapTitled 754 µs / JoinVertical 683 µs / total ~2.4-2.8 ms/op at 200×60) was retained as informative input to Phase 11 SC2's caching design.

## Issues Encountered

None blocking.

The PreToolUse Edit hook fired three READ-BEFORE-EDIT reminders during execution. Each time the file in question had already been read in full at agent startup (plan-context-load step) — the runtime allowed the edits to land successfully (the hook is a soft reminder, not a block). All four edits landed in the intended atomic commit.

## Threat Surface Scan

No new trust boundaries introduced. This plan is pure governance + doc + test-skip — no production code path modified, no new network endpoints, auth paths, file access patterns, or schema changes.

The threat model from Phase 7 Plan 03 (T-7-03-01..T-7-03-08) remains accurate; specifically T-7-03-04 (DoS / bench test adds 1-2s to CI) is now mitigated to zero CI cost since the test t.Skips unconditionally.

## TDD Gate Compliance

This plan declared `tdd="false"` in the task header. Pure governance / revert / audit-trail work — no behavior contract to test, no RED/GREEN/REFACTOR cycle applies. The "test" being modified (TestBenchmarkAppView_UnderBudget) is itself the gate; we are intentionally rendering it inert via t.Skip until Phase 11 SC2 closes the gap.

## Forward Note for Plan 05

**D-105 is explicitly Plan 05's responsibility, NOT this plan's.**

This plan closes Phase 7's *governance* gap. The verification record at `.planning/phases/07-chrome-skeleton/07-VERIFICATION.md` still says `status: gaps_found` because Plan 05 (the closing wave of Phase 7.1) is the appropriate place to flip:

- `status: gaps_found` → `status: gaps_closed_by_phase_7.1`
- Add this gap to `re_verification.gaps_closed`

Plan 05 should run AFTER all other Phase 7.1 plans (02-WR-01 AST walker, 03-WR-02 nested-borders, 04-WR-03 narrow-terminal, etc.) so the verifier sees the full closure picture in one update.

Until Plan 05 lands, the verification status remains `gaps_found` even though this BLOCKER is resolved — that is intentional and correct sequencing.

## Next Plan Readiness

**Phase 7.1 Plan 02..04 may now proceed in their respective waves:**

- The chrome contract is honest again — locked SC5 target is in ROADMAP, in code, and in the audit trail
- `TestBenchmarkAppView_UnderBudget` is t.Skipped; Phase 7.1 Plan 02 (BFS AST walker rewrite) can extend `TestViewNoNewStyle` without worrying about the bench gate
- 07-03-SUMMARY.md historical record is preserved; future planners see both the original deviation rationale AND its eventual resolution

## Self-Check: PASSED

Verified post-write:

- ✓ `.planning/ROADMAP.md` line 192 matches locked CONTEXT.md D-24 verbatim (`grep -F 'BenchmarkAppView\` stays ≤ 50 µs/op at 200×60' .planning/ROADMAP.md` returns 2 lines: SC5 + Phase 11 SC2)
- ✓ `internal/app/chrome_test.go` has `const budgetNs = 50_000` (1 match) and `const budgetNs = 5_000_000` (0 matches)
- ✓ `internal/app/chrome_test.go` has the verbatim D-103 t.Skip message at top of TestBenchmarkAppView_UnderBudget
- ✓ `.planning/phases/07-chrome-skeleton/07-DISCUSSION-LOG.md` carries `## [APPROVED] 2026-04-27 — Phase 7.1 SC5 governance restoration` heading + accepted_by + accepted_at + Phase 11 owner + cross-references
- ✓ `.planning/phases/07-chrome-skeleton/07-03-SUMMARY.md` Rule 1 deviation block has the Closure (Phase 7.1, 2026-04-27) sub-bullet (1 match); historical record preserved (the original deviation block is still present)
- ✓ Commit `444df3b` exists in `git log` (atomic — all 4 files in one commit)
- ✓ `go build ./...` exits 0
- ✓ `go vet ./internal/app/...` exits 0
- ✓ `go test ./internal/app -run TestBenchmarkAppView_UnderBudget -count=1 -v` reports `--- SKIP` with the verbatim deferral message (not PASS, not FAIL)
- ✓ `go test ./... -count=1` exits 0 (full repo green; the t.Skip avoids running the bench against the strict budget)

---

*Phase: 07.1-chrome-gap-closure*
*Plan: 01 — SC5 governance restoration*
*Completed: 2026-04-27*
